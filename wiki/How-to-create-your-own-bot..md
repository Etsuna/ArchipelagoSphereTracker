# How to create your own AST bot

This legacy Wiki address is kept so existing links do not break.

The complete and current procedure—Discord application, OAuth2 permissions, `.env`, Windows/Linux startup, updates, and systemd—is now available on [[Installation]].

La procédure complète et actuelle—application Discord, permissions OAuth2, `.env`, démarrage Windows/Linux, mises à jour et systemd—se trouve maintenant dans [[Installation]].
